<h1>Products</h1>
<ul>
  <% @cart.each do |item| %>
    <li><%= item %></li>
  <% end %>
</ul>

<%= form_tag("/", method: "get") do %>
  <%= label_tag(:item, "Add item to cart:") %>
  <%= text_field_tag(:item) %>
  <%= submit_tag("Add to cart") %>
<% end %>
