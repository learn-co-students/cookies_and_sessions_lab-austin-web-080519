<h2>Add Items:</h2>
    <%= form_tag('/') %>
    <%= text_field_tag :product %>
    <%= submit_tag "add to cart"%>

<h2>Your Cart:</h2>
    <ul>
        <% session[:cart].each do |i| %>
            <li><%= i %></li>
        <% end %>
    </ul>